{
    "folders": [
        {
            "path": "."
        }
    ],
    "settings": {
        "tab_size": 2
    },
    "build_systems": [
        {
            "name": "aoapc",
            "cmd": [
                "g++ -Wall ${file} -o ${file_base_name} && [ -f ${file_base_name}.in ] && ./${file_base_name} < ./${file_base_name}.in || ./${file_base_name}"
            ],
            "working_dir": "${file_path}",
            "selector": "source.c++",
            "shell": true
        }
    ]
}
